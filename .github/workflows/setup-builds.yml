name: start map builds

on: 
  workflow_dispatch:
  schedule:
    - cron: "0 0 1 * *"
    - cron: "0 0 15 * *"

jobs:

  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0 

      - name: edit Readme
        run: |
          sed -i "1s/.*/# Last Map Update: $(date)/"  README.md

      - name: Generate tag
        id: tag
        run: echo "tag=$(date +'%Y-%m-%d')" >> "$GITHUB_OUTPUT"
      
      - name: Commit, Tag and Push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add README.md
          git tag ${{ steps.tag.outputs.tag }}
          git commit -m "Update map creation date"
          git push origin master --tags
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          files: README.md
          body: |
            ## Map build

            These maps are generated from OpenStreetMap data extracts provided by Geofabrik:
            https://download.geofabrik.de/

            Data © OpenStreetMap contributors  
            Licensed under the Open Database License (ODbL):
            https://opendatacommons.org/licenses/odbl/1-0/

            Please credit OpenStreetMap when using these maps:
            © OpenStreetMap contributors (https://www.openstreetmap.org/copyright)

  build-osmosis-cache:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: omsis-cache
        uses: actions/cache/restore@v4
        id: cache
        with:
          path: osmosis
          key: osmosis-cache

      - name: omsis
        if: steps.cache.outputs.cache-hit != 'true'
        run: sh setup_env.sh

      - uses: actions/cache/save@v4
        if: steps.cache.outputs.cache-hit != 'true'
        with:
          path: osmosis
          key: osmosis-cache

  # cleanup-old-workflows:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: cleanup
  #       env:
  #         GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #       run: gh run list --repo ${{ github.repository }} --limit 1000 --json databaseId -q '.[].databaseId' | while read id; do gh run delete "$id" --repo ${{ github.repository }} || continue; done

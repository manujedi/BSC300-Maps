name: poland

env:
  countrycode: PL

on:
  workflow_dispatch:
  workflow_run:
    workflows: [start map builds]
    types:
      - completed

jobs:
  build:
    runs-on: ubuntu-latest
    continue-on-error: true

    strategy:
      matrix:
        maps:
          [
            {name: dolnoslaskie, url: https://download.geofabrik.de/europe/poland/dolnoslaskie-latest.osm.pbf, state: "0100"},
            {name: kujawsko-pomorskie, url: https://download.geofabrik.de/europe/poland/kujawsko-pomorskie-latest.osm.pbf, state: "0200"},
            {name: lodzkie, url: https://download.geofabrik.de/europe/poland/lodzkie-latest.osm.pbf, state: "0300"},
            {name: lubelskie, url: https://download.geofabrik.de/europe/poland/lubelskie-latest.osm.pbf, state: "0400"},
            {name: lubuskie, url: https://download.geofabrik.de/europe/poland/lubuskie-latest.osm.pbf, state: "0500"},
            {name: malopolskie, url: https://download.geofabrik.de/europe/poland/malopolskie-latest.osm.pbf, state: "0600"},
            {name: mazowieckie, url: https://download.geofabrik.de/europe/poland/mazowieckie-latest.osm.pbf, state: "0700"},
            {name: opolskie, url: https://download.geofabrik.de/europe/poland/opolskie-latest.osm.pbf, state: "0800"},
            {name: podkarpackie, url: https://download.geofabrik.de/europe/poland/podkarpackie-latest.osm.pbf, state: "0900"},
            {name: podlaskie, url: https://download.geofabrik.de/europe/poland/podlaskie-latest.osm.pbf, state: "1000"},
            {name: pomorskie, url: https://download.geofabrik.de/europe/poland/pomorskie-latest.osm.pbf, state: "1100"},
            {name: slaskie, url: https://download.geofabrik.de/europe/poland/slaskie-latest.osm.pbf, state: "1200"},
            {name: swietokrzyskie, url: https://download.geofabrik.de/europe/poland/swietokrzyskie-latest.osm.pbf, state: "1300"},
            {name: warminsko-mazurskie, url: https://download.geofabrik.de/europe/poland/warminsko-mazurskie-latest.osm.pbf, state: "1400"},
            {name: wielkopolskie, url: https://download.geofabrik.de/europe/poland/wielkopolskie-latest.osm.pbf, state: "1500"},
            {name: zachodniopomorskie, url: https://download.geofabrik.de/europe/poland/zachodniopomorskie-latest.osm.pbf, state: "1600"},
          ]

        variant:
          - name: no-street-names
            tags: tags_minimal_street_only.xml
            zip: no-street-names
            drop_names: true
            keep_extra: ""
            modify_extra: ""

          - name: with-street-names
            tags: tags_minimal_street_only.xml
            zip: with-street-names
            drop_names: false
            keep_extra: ""
            modify_extra: ""

          - name: with-water
            tags: tags_with_water.xml
            zip: with-water
            drop_names: false
            keep_extra: '--keep="waterway= landuse= natural="'
            modify_extra: ""

          - name: with-green-stuff
            tags: tags_with_green_stuff.xml
            zip: with-green-stuff
            drop_names: false
            keep_extra: '--keep="waterway= landuse= natural= leisure="'
            modify_extra: |
              leisure=garden to landuse=grass \
              leisure=playground to landuse=grass \
              leisure=park to landuse=grass \
              landuse=orchard to =grass \
              landuse=allotments to =grass \
              landuse=farmland to =grass \
              landuse=flowerbed to =grass \
              landuse=meadow to =grass \
              landuse=plant_nursery to =grass \
              landuse=vineyard to =grass \
              landuse=greenfield to =grass \
              landuse=village_green to =grass \
              landuse=greenery to =grass \
              landuse=cemetery to =grass \
              natural=scrub to landuse=grass \

    steps:
      - name: checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Env
        run: |
          sudo apt-get update 
          sudo apt-get install wget openjdk-21-jre python3 pip
          pip install numpy
          echo "freeing ~10gb of disk space"
          sudo rm -rf /usr/local/lib/android || true

      - name: omsis-cache
        uses: actions/cache/restore@v4
        id: cache
        with:
          path: osmosis
          key: osmosis-cache

      - name: omsis
        if: steps.cache.outputs.cache-hit != 'true'
        run: sh setup_env.sh

      - name: Download
        run: |
          wget ${{ matrix.maps.url }} -O tmp.pbf

      - name: Convert for filtering
        run: |
          ./osmosis/osmconvert tmp.pbf -o=tmp.o5m
          rm tmp.pbf

      - name: filter
        run: |
          osmosis/osmfilter -v tmp.o5m \
          --keep="highway=primary =primary_link =secondary =secondary_link =tertiary =tertiary_link =trunk =trunk_link =cycleway =living_street =residential =road =track =unclassified" \
          --keep="highway=service and ( bicycle=designated or bicycle=yes or bicycle=permissive )" \
          --keep="highway=footway and ( bicycle=designated or bicycle=yes or bicycle=permissive )" \
          --keep="highway=bridleway and ( bicycle=designated or bicycle=yes or bicycle=permissive )" \
          --keep="highway=path and ( bicycle=designated or bicycle=yes or bicycle=permissive )" \
          --keep="highway=pedestrian and ( bicycle=designated or bicycle=yes or bicycle=permissive )" \
          --keep="highway=unclassified and ( bicycle=designated or bicycle=yes or bicycle=permissive )" \
          --keep="sidewalk:*:bicycle=yes" \
          --keep="route=bicycle =mtb" \
          --keep="cycleway:*=lane :*=track *:=shared_lane *:=share_busway *:=separate *:=crossing *:=shoulder *:=link *:=traffic_island" \
          --keep="bicycle_road=yes" \
          --keep="cyclestreet=yes" \
          ${{ matrix.variant.keep_extra }} \
          ${{ matrix.variant.drop_names && '--drop-tags="name= ref="' || '' }} \
          --out-o5m > tmp1.o5m

          osmosis/osmfilter -v tmp1.o5m --modify-tags=" \
          highway=trunk_link to =primary \
          highway=trunk to =primary \
          highway=primary_link to =primary \
          highway=tertiary_link to =tertiary \
          highway=secondary_link to =secondary \
          highway=trunk_link to =trunk \
          highway=footway to =cycleway \
          highway=bridleway to =cycleway \
          highway=sidewalk to =cycleway \
          highway=path to =cycleway \
          highway=pedestrian to =cycleway \
          highway=unclassified to =cycleway \
          ${{ matrix.variant.modify_extra }} \
          " \
          --drop-author --drop-version --out-o5m > tmp_filtered.o5m

          rm tmp.o5m tmp1.o5m

      - name: Convert for osmosis 
        run: |
          ./osmosis/osmconvert tmp_filtered.o5m -o=tmp_filtered.pbf
          rm tmp_filtered.o5m

      - name: Build mapsforge map
        run: |
          /usr/bin/time -v python generate_map.py -i tmp_filtered.pbf -c $countrycode -s ${{ matrix.maps.state }} -t ${{ matrix.variant.tags }}

      - name: zip
        run: |
          zip -r ${{ github.workflow }}-${{ matrix.maps.name }}-${{ matrix.variant.zip }}.zip *.map

      - name: get tag
        id: tag
        run: |
          git fetch --force --tags
          git tag
          echo "tag=$(git describe --tags --abbrev=0)" >> "$GITHUB_OUTPUT"

      - name: Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          files: ./*.zip
